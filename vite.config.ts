import { defineConfig } from 'vite';

// Served from https://neopitch.naffiq.com/ on GitHub Pages, so asset
// URLs must be prefixed with the repo name. Locally `vite dev` ignores this.
export default defineConfig({
});
